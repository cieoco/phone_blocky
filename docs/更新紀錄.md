# ESP32 Phone Blocky 更新紀錄

## 2026年6月16日 更新

### 🤖 馬達調校工具：自動估速度 PID 全面修正

#### ✅ 修正錯誤的 PID 增益（主因）
- 自動估算出的 Kp/Ki 因「duty 分數 vs 百分比」單位不一致而**小了 100 倍**，PID 形同失效（純 PID 收斂要 >10 秒）。
- 換算成百分比單位後對齊韌體輸出，閉迴路收斂恢復正常。

#### ✅ 量測精度
- 新增韌體 **50Hz 高速擷取緩存**（`chart_buffer_start/stop`），取代 10Hz telemetry，能解析馬達上升段。
- τ 改用第一個「從 0 起步」的準位段估計，避免落在 0.05s 地板值。

#### ✅ 前饋與速度環
- kS 允許小負截距，前饋不再於目標轉速處系統性偏高。
- 韌體速度環加入**目標斜坡、積分抗飽和、前饋非負夾**（移植自 motorControl）。
- 自動估完成後前饋自動套用 RAM。

#### ✅ 安裝相依
- 放寬 `numpy` 版本上限（移除 `<2.0`）、`matplotlib>=3.9`，Python 3.13 可直接安裝、不需 C 編譯器。

#### 📄 文件
- 重寫 [安裝教程](安裝教程.md)：依實際工具更新「開啟調校工具 / 燒錄韌體 / 自動估 PID」流程（含 Baud、斷線後燒錄、清除 flash 等）。
- 同步更新 [motor_tuner 說明](../motor_tuner/README.md)：補上「燒錄」分頁與「自動估 PID」功能。

---

## 2025年9月29日 更新

### 🎯 WiFi 設定頁面重大改版 (set.html)

#### ✅ 新增多選設定功能
- **設定項目選擇**：使用者可透過多選框選擇要修改的項目
  - 📡 修改 WiFi 連線設定
  - 🏷️ 修改控制板名稱
  - 支援同時選擇或單獨選擇

#### ✅ 智能動態介面
- **動態顯示**：根據選擇項目自動顯示/隱藏相關欄位
- **按鈕文字變化**：
  - 兩項都選：「儲存 WiFi 與名稱設定」
  - 僅WiFi：「儲存 WiFi 設定」
  - 僅名稱：「儲存控制板名稱」
- **說明文字更新**：即時顯示當前選擇的操作說明

#### ✅ 預設 WiFi 設定
- **SSID**：留空（改由 set.html 設定）
- **密碼**：留空（請自行填入，勿提交真實密碼）
- **儲存機制**：使用 ESP32 Preferences 庫存於 flash NVS 區域

#### ✅ 後端路由支援
- `/setwifi`：完整 WiFi 和名稱設定
- `/setnameonly`：僅控制板名稱設定
- 自動驗證和錯誤處理

### 🎯 硬體測試頁面優化 (hardware_test.html)

#### ✅ LEGO 按鈕測試修正
- **添加缺失顯示元素**：補充即時數值顯示區域
- **修正 JavaScript**：更新按鈕數據處理邏輯
- **增強用戶提示**：添加明確的操作指引

#### ✅ 匯出功能修正
- **修正測試結果鍵名**：統一所有測試項目的資料鍵名
  - M1 馬達：`progress-m1` → `m1`
  - M2 馬達：`progress-m2` → `m2`
  - LEGO 按鈕：`progress-button` → `button`
- **完整匯出支援**：所有 6 個測試項目都能正確匯出到報告

#### ✅ 介面統一性
- **回首頁按鈕**：統一為紅色漸層格式，與其他頁面保持一致
  - 移除舊的 CSS 樣式
  - 採用 inline 樣式確保一致性
  - 使用 🏠 emoji 圖標

### 🎯 系統訊息統一 (blockly.html)

#### ✅ 文字大小統一
- **動態訊息**：統一為 14px (從 0.9em 改為固定像素)
- **初始文字**：「等待ESP32回應中...」設為 14px
- **使用說明**：所有操作提示文字統一為 14px
- **確保視覺一致性**：消除不同大小文字混雜的問題

### 🔧 技術架構改進

#### ✅ 資料持久化
- **WiFi 設定儲存**：使用 ESP32 Preferences 庫
- **儲存位置**：NVS (Non-Volatile Storage) flash 區域
- **自動重連**：開機自動嘗試連接已設定的 WiFi

#### ✅ 前後端整合
- **WebSocket 通訊**：硬體測試即時數據傳輸
- **RESTful API**：設定頁面的 POST 請求處理
- **錯誤處理**：完善的驗證和反饋機制

### 📁 檔案變更清單

#### 修改的檔案：
1. **data/set.html** - WiFi 設定頁面重構
2. **data/hardware_test.html** - 硬體測試功能完善
3. **data/blockly.html** - 文字大小統一
4. **data/app.js** - 系統訊息字體修正
5. **src/WebServerHandler.h** - 新增 `/setnameonly` 路由

#### 新增功能：
- 多選設定模式
- 動態介面更新
- 完整測試報告匯出
- 統一視覺設計

### 🎯 使用者體驗改進

#### ✅ 操作簡化
- 一鍵選擇設定項目
- 清晰的視覺反饋
- 防呆機制和驗證

#### ✅ 視覺統一
- 統一的按鈕樣式
- 一致的文字大小
- 協調的色彩配置

#### ✅ 功能完整性
- 全面的硬體測試
- 完整的設定選項
- 準確的報告匯出

---

## 下一步規劃

### 🔮 可能的改進方向
- [ ] 添加更多感測器支援
- [ ] 增強錯誤診斷功能
- [ ] 優化行動裝置體驗
- [ ] 添加使用者偏好設定

### 🛠️ 技術債務
- [ ] 程式碼模組化重構
- [ ] 單元測試覆蓋
- [ ] 效能最佳化
- [ ] 文件完善

---

*更新人員：GitHub Copilot*  
*更新日期：2025年9月29日*